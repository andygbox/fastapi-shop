[project]
name = "backend"
version = "0.1.0"
description = "Backend for FastAPI Shop"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.121.2",
    "sqlalchemy>=2.0.44",
    "uvicorn>=0.38.0",
]
